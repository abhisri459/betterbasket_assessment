# Data

Place the provided assessment CSV files here before running the matcher:

- `products_A.csv`
- `products_B.csv`

The raw CSV files are intentionally ignored by Git because they are large input
artifacts supplied with the assessment.
